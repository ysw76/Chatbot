# 🏥 육아맘 의료진 찾기 — 설계 전략

## 1. 전체 구조

```
사용자 (브라우저)
    ↕
Streamlit 서버 (Python)
    ├── 병원 정보 검색 (JSON 기반)
    └── 사용자 상담 정보 관리
```

병원 정보를 JSON 파일로 관리하여 간단하고 빠른 검색을 제공한다.

---

## 2. 핵심 기능

### 2-1. 아이 정보 수집
- 엄마 이름
- 아이 나이
- 연락처
- 아이 증상/필요 진료과 선택

### 2-2. 병원 검색
- 진료과별 검색 (소아청소년과, 가정의학과)
- 증상별 추천 (감기, 발열, 기침, 구토, 피부질환, 예방접종)
- 주말 진료 병원 우선 표시
- 상세 정보: 병원명, 주소, 전화, 진료시간(평일/토요일/일요일/공휴일), 의사명

### 2-3. 검색 기록 관리
- 검색 기록 저장 (JSON 파일)
- 통계 정보 제공 (총 메시지 수, 사용자/의료진 메시지 분류)

---

## 3. 데이터 구조

### 병원 데이터 (hospitals_data.json)

```json
{
  "hospitals": [
    {
      "id": 1,
      "name": "병원명",
      "category": "소아청소년과",
      "address": "충남 천안시 ...",
      "phone": "041-555-0001",
      "doctor": "의사명 또는 상담 가능",
      "weekday_time": "09:00 ~ 18:00",
      "saturday_time": "09:00 ~ 13:00",
      "sunday_time": "휴무",
      "holiday_time": "휴무",
      "notes": "특이사항"
    }
  ]
}
```

---

## 4. 파일 구조

```
Chatbot/
├── main.py                      Streamlit UI (색상 디자인 포함)
├── hospitals_search.py          병원 검색 모듈 (JSON 기반)
├── hospitals_data.json          병원 정보 데이터
├── requirements.txt             의존성
├── 설계전략.md                  이 파일
└── search_*.json                검색 기록 (자동 저장)
```

---

## 5. 핵심 모듈

### main.py
- Streamlit 기반 UI
- 사용자 정보 입력 폼
- 검색 결과 표시
- 검색 기록 저장 기능
- 색상 디자인: 파란색(#7FC0FA), 노란색(#F0FA73), 빨간색(#FA2E23)

### hospitals_search.py
- `load_hospitals()`: JSON에서 병원 정보 로드
- `search_by_category(category)`: 진료과별 검색
- `search_weekend_hospitals()`: 주말 진료 병원 검색
- `search_by_symptom(symptom)`: 증상별 추천
- `query_hospitals(question)`: 사용자 질문 기반 통합 검색
- `format_hospital_info(hospitals)`: 병원 정보 포맷팅

---

## 6. 검색 로직

```
사용자 입력
  ↓
hospitals_search.query_hospitals()
  ├─ "주말" 포함 → search_weekend_hospitals()
  ├─ "소아청소년과" 포함 → search_by_category("소아청소년과")
  ├─ "가정의학과" 포함 → search_by_category("가정의학과")
  ├─ 증상 포함 → search_by_symptom(symptom)
  └─ 기본값 → 전체 병원 표시
  ↓
형식화된 병원 정보 반환
  ↓
UI에 표시
```

---

## 7. 사용 방법

### 로컬 개발

```bash
# 의존성 설치
pip install -r requirements.txt

# 앱 실행
streamlit run main.py
```

### 데이터 업데이트

1. `hospitals_data.json` 파일을 수정하여 병원 정보 추가/수정
2. 앱 재시작 시 자동으로 반영

---

## 8. 색상 팔레트

| 색상 | HEX | RGB | 용도 |
|------|-----|-----|------|
| 파란색 | #7FC0FA | 127, 192, 250 | 사용자 메시지, 테두리 |
| 노란색 | #F0FA73 | 240, 250, 115 | 강조, 경고 |
| 빨간색 | #FA2E23 | 250, 46, 35 | 중요 정보 |
| 어두운 빨강 | #A8120B | 168, 18, 11 | 텍스트, 섀도우 |
| 회색 | #607080 | 96, 112, 128 | 중립, 보조 |
| 회갈색 | #787A5B | 120, 122, 91 | 배경 |

---

## 9. Claude API 통합 (선택사항)

### 모드 선택
- **기본 모드**: hospitals_search 기반 병원 검색 (항상 사용 가능)
- **Claude 모드**: AI 기반 자연스러운 상담 (API 키 입력 시 활성화)

### Claude 상담 특징
```
사용자 입력
  ↓
Claude API 호출 (model: claude-3-5-sonnet)
  ↓
- 증상 공감 및 상세 진청
- 응급 증상 판단
- 병원 추천
- 건강 관리 조언
  ↓
자연스러운 대화형 응답
```

### 보안
- API 키는 Streamlit 세션 상태에만 저장 (브라우저에 노출 안 됨)
- `.env` 파일에 저장하면 자동 로드
- 환경변수: `ANTHROPIC_API_KEY`

---

## 10. 확장 포인트

### 진행 완료 ✅
- ✅ 지역별 검색 (서북구, 동남구)
- ✅ Claude API 통합 (선택사항)
- ✅ 빠른 검색 버튼
- ✅ 필터 조합 검색

### 향후 계획
- **위치 기반 검색**: 사용자 현재 위치 기반 병원 추천
- **예약 기능**: 선택한 병원으로 전화 또는 온라인 예약
- **리뷰 기능**: 병원 방문 후 평가 및 리뷰 작성
- **데이터베이스 연동**: JSON → SQLite/PostgreSQL 마이그레이션
- **병원 API 연동**: 실시간 진료시간/예약 가능 여부 확인
- **다국어 지원**: 영어, 중국어 등 추가

